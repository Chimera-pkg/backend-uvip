# ============================================================
# UVIP — Environment Variables (.env)
# ============================================================
# Copy file ini ke .env di VPS, lalu GANTI semua value-nya!
# JANGAN commit file .env ke Git!
# ============================================================

# ---- Database ----
DB_PASSWORD=uvip_secret_2026

# ---- JWT Secret ----
# Generate random string:  python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=ganti_ini_dengan_random_string_yang_panjang

# ---- Token expiry ----
ACCESS_TOKEN_EXPIRE_MINUTES=120
